import AsyncStorage from "@react-native-async-storage/async-storage"

class GeneralStore {
  constructor() {
  }

  async checkIfFirstOpen() {
    // const  = await AsyncStorage.getItem()
  }
}
export default GeneralStore